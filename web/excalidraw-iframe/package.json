{
  "name": "codedistill-excalidraw-iframe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Minimal React+Excalidraw wrapper for the CodeDistill rich-canvas Slice 4. Built separately from the main Svelte SPA so React + Excalidraw never enter that bundle. Communicates with the host via postMessage.",
  "scripts": {
    "build": "vite build",
    "dev": "vite"
  },
  "dependencies": {
    "@excalidraw/excalidraw": "^0.17.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
